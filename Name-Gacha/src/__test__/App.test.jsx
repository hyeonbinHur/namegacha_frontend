// import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('Example', () => {
    it('simple test', () => {
        expect(1 + 1).toBe(2);
    });
});
